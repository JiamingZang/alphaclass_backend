# 配置文件模板：复制为 application.yml 并填写真实环境变量后使用。
# 真实 application.yml 已被 .gitignore 忽略，不会提交到仓库。
spring:
  datasource:
    url: jdbc:mysql://${DB_HOST:localhost}:${DB_PORT:3306}/alphaclass?useSSL=false&serverTimezone=Asia/Shanghai&useUnicode=true&characterEncoding=utf-8&autoReconnect=true
    username: ${DB_USERNAME:root}
    password: ${DB_PASSWORD}
server:
  port: ${SERVER_PORT:8080}
app:
  base-url: ${APP_BASE_URL:https://SERVER_IP_PLACEHOLDER/v2}
ai:
  baidu:
    client-id: ${BAIDU_CLIENT_ID}
    client-secret: ${BAIDU_CLIENT_SECRET}
  zhipu:
    api-key: ${ZHIPU_API_KEY}
  tencent:
    secret-id: ${TENCENT_SECRET_ID}
    secret-key: ${TENCENT_SECRET_KEY}
  oss:
    endpoint: ${OSS_ENDPOINT:oss-cn-beijing.aliyuncs.com}
    bucket: ${OSS_BUCKET:alphaclass}
    access-key-id: ${OSS_ACCESS_KEY_ID}
    access-key-secret: ${OSS_ACCESS_KEY_SECRET}
  youdao:
    app-key: ${YOUDAO_APP_KEY}
    app-secret: ${YOUDAO_APP_SECRET}
logging:
  level:
    root: INFO
